formulas:
    scratch: (scope * correctness / 25.0 * 9 + 1).round(1)
    c: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    crypto: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    fifteen: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    forensics: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)

calculation:
    resit:
        resit: 0
        psets: 100
    normal:
        quiz: 0
        psets: 100

psets:
    type: float
    drop: scope
    grades:
        scratch: 1
        c: 1
        crypto: 1
        fifteen: 1
        forensics: 1

quiz:
    type: pass
    required: true
    grades:
        quiz: 1

resit:
    type: pass
    required: true
    grades:
        resit: 1
