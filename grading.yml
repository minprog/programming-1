formulas:
    scratch: (scope * correctness / 25 * 9 + 1).round(1)
    c: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    crypto: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    breakout: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)
    forensics: (scope * (3 * correctness + 2 * design + 1 * style) / 150.0 * 9 + 1).round(1)

calculation:
    resit:
        resit: 0
        psets: 70
        passes: 30
    normal:
        quiz: 0
        psets: 70
        passes: 30

psets:
    type: integer
    drop: correctness
    grades:
        c: 1
        crypto: 1
        breakout: 1
        forensics: 1

passes:
    type: pass
    grades:
        scratch: 1

quiz:
    type: pass
    required: true
    grades:
        quiz: 1

resit:
    type: pass
    required: true
    grades:
        resit: 1
